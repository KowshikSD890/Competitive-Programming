
#include<stdio.h>
int main(){
    int t;
    scanf("%d",&t);
for(int tc = 1 ; tc <= t ; tc++){

    int x,y;
    scanf("%d%d",&x,&y);

    int z = ((y + 99)  - x * 100 ) / 100;
    if( z > 0){
        printf("%d\n",z);
    }
    else
        printf("0\n");
}
return 0;
}

